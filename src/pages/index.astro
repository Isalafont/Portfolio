---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Isabelle Lafont — Développeuse & Experte RGAA">
  <main>
    <header>
      <div class="hero-content">
        <div class="kicker">Développeuse Ruby on Rails <span>&</span> Experte RGAA</div>
        <h1>Isabelle Lafont</h1>
        <p>
          Je construis des produits numériques robustes, accessibles et pilotés par l'IA. 
          Actuellement chez <strong>BetaGouv</strong> sur le projet <strong>DataPass</strong>.
        </p>

        <div class="contact-chips">
          <span class="location" role="img" aria-label="Localisation">📍 Paris - Romainville</span>
          <a href="mailto:isabelle@example.com" class="email">📧 Me contacter</a>
        </div>
      </div>

      <div class="hero-visual">
        <img src="/profile.svg" alt="Illustration stylisée d'Isabelle" class="profile-avatar" />
      </div>
    </header>

    <section class="projects-preview">
      <div class="card">
        <h2>RGAA Run</h2>
        <p>Challenge ton accessibilité avec notre nouveau module interactif.</p>
        <a href="/projects/accessibilite">Relever le défi →</a>
      </div>
    </section>

    <footer>
      <div class="status">
        <span class="dot"></span> Disponible pour de nouvelles missions AI-First
      </div>
      <div class="links">
        <a href="https://github.com/Isalafont">GitHub</a>
        <a href="https://twitter.com/Isalafont">Twitter</a>
      </div>
      <p class="credits">© 2026 Isabelle Lafont — Code & Inclusion.</p>
    </footer>
  </main>
</Layout>

<style>
  main {
    max-width: 800px;
    margin: 0 auto;
    padding: 4rem 2rem;
  }

  header {
    display: flex;
    flex-direction: column-reverse;
    gap: 2rem;
    margin-bottom: 5rem;
  }

  @media (min-width: 768px) {
    header {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
    }
  }

  .hero-content {
    flex: 1;
  }

  .hero-visual {
    flex-shrink: 0;
    width: 200px;
    height: auto;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .hero-visual {
      margin: 0;
    }
  }

  .profile-avatar {
    width: 100%;
    height: auto;
    filter: drop-shadow(var(--shadow));
  }

  .kicker {
    font-size: 0.9rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--muted);
    margin-bottom: 1rem;
  }

  .kicker span {
    color: var(--accent);
  }

  h1 {
    font-size: 2.5rem;
    color: var(--text);
    margin-bottom: 2rem;
    max-width: 15ch;
  }

  p {
    font-size: 1.25rem;
    line-height: 1.5;
    margin-bottom: 1.5rem;
  }

  .projects-preview .card {
    background: white;
    padding: 2rem;
    border-radius: 1rem;
    box-shadow: var(--shadow);
    margin-top: 4rem;
  }

  .projects-preview h2 {
    margin-top: 0;
    margin-bottom: 0.5rem;
    font-size: 1.25rem;
  }

  .projects-preview p {
    color: var(--muted);
    margin-bottom: 1.5rem;
  }

  .projects-preview a {
    font-weight: 700;
    text-decoration: none;
  }

  .contact-chips {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 3rem;
    font-size: 1rem;
  }

  .contact-chips .location {
    color: var(--muted);
  }

  .contact-chips .email {
    text-decoration: none;
    color: var(--muted);
  }

  .contact-chips .email:hover {
    color: var(--accent);
  }

  footer {
    margin-top: 8rem;
    color: var(--muted);
    font-size: 0.85rem;
  }
</style>
